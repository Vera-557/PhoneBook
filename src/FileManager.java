import java.util.List;
import java.util.Scanner;

public class FileManager {
    String USER_FILE = "user.txt";//список всех юзеров
    String CONTACTS_DIR = "contacts.txt";//у каждого юзера свой файл контаков
    String LOG_FILE = "logger.txt";// 1 общий для всех юзеров

    static List<User> loadUsers (Scanner scanner) {
        return null;
    }
    //читать и парсить строки
    static void saveUsers(List <User> user){}
    //записать, перезаписать файл со списком пользователя

}
